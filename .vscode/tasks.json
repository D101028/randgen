{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "CMake Build",
      "type": "shell",
      "command": "cmake -G \"MinGW Makefiles\" -S . -B build \ncmake --build build",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "detail": "Generate and build with MinGW Makefiles"
    }
  ]
}
